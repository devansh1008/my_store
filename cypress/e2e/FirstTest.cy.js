describe("Home Page Tests", () => {
  // beforeEach(() => {
  //   cy.visit("/"); // Visit the Home page
  // });

  it("should load the page and find the header", () => {
    cy.visit("/");
  });
});
